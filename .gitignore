# dependencies
node_modules/
worker/node_modules/

# build output
dist/

# local env (endpoint URL, etc.) — keep .env.example, ignore the real one
.env
.env.*
!.env.example

# Cloudflare Wrangler
worker/.wrangler/
worker/.dev.vars

# raw video sources — only the compressed clips in public/video/ ship
src/video/

# misc
.DS_Store
*.log
